```

1. Check vm.swappiness on all your nodes Set the value to 1 if necessary
2. Show the mount attributes of all volumes
3. Show the reserve space of any non-root, ext-based volumes XFS volumes do not use reserve space
4. Disable transparent hugepages
5. List your network interface configuration
6. List forward and reverse host lookups using getent or nslookup
7. Show the nscd service is runningShow the ntpd service is running


```
